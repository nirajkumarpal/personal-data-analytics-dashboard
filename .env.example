# Example environment variables for local development and Render
# Copy to a `.env` file locally (do NOT commit `.env`)

SECRET_KEY=change-me-to-a-secure-random-value
DATABASE_URL=sqlite:///personal_analytics.db
FLASK_DEBUG=0
PORT=5000
